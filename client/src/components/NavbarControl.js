import React from "react";

import { IconButton } from "./common";

import "./NavbarControl.css";

const NavbarControl = () => (
  <div className="navbar-control">
    {/*
      <IconButton
        iconClassName="oi-envelope-closed"
        onClick={() => {
          console.log("clicked");
        }}
        tooltip="Sign up for our Newsletter!"
      />
    */}
    {
    //<IconButton
      //iconClassName="oi-bell"
      //onClick={() => {
        //console.log("clicked");
      //}}
    ///>
    }
  </div>
);

export default NavbarControl;
